# Generated file - DO NOT EDIT
# Source: deploy-discord-bot.yml.genie.ts

name: Deploy Discord bot

on:
  workflow_dispatch:
    inputs:
      stage:
        description: Cloudflare stage (production requires separate stack admission)
        required: true
        type: choice
        options: [staging]

permissions:
  contents: read
  id-token: write

concurrency:
  group: 'discord-bot-deploy-${{ inputs.stage }}'
  cancel-in-progress: false

env:
  CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
  CI: 'true'
  FORCE_SETUP: '1'

jobs:
  deploy:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    environment: ${{ inputs.stage }}
    defaults:
      run:
        shell: bash
    env:
      STAGE: ${{ inputs.stage }}
      RELEASE_ID: ${{ github.sha }}
      CF_WORKER_NAME: ${{ vars.CF_WORKER_NAME }}
      CF_BOT_STATE_NAMESPACE_ID: ${{ vars.CF_BOT_STATE_NAMESPACE_ID }}
      CF_WORKER_URL: ${{ vars.CF_WORKER_URL }}
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
          ref: ${{ github.sha }}
      - name: Install Nix
        uses: DeterminateSystems/determinate-nix-action@v3
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          extra-conf: |
            experimental-features = nix-command flakes
            accept-flake-config = true
            extra-substituters = https://devenv.cachix.org
            extra-trusted-public-keys = devenv.cachix.org-1:w1cLUi8dv3hnoSPGAuibQv+f9TZLr6cv/Hm9XgU50cw=
            access-tokens = github.com=${{ github.token }}
            extra-substituters = https://cache.nixos.org
            extra-trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY=
          summarize: true
      - name: Sync megarepo dependencies
        env:
          GITHUB_TOKEN: ${{ github.token }}
          MEGAREPO_STORE: '${{ runner.temp }}/megarepo-store/${{ github.run_id }}/${{ github.run_attempt }}/${{ github.job }}'
        run: |
          EU_REV=$(jq -r '.members["effect-utils"].commit' megarepo.lock)
          if [ -z "$EU_REV" ] || [ "$EU_REV" = "null" ]; then
            echo '::error::megarepo.lock missing members["effect-utils"].commit'
            exit 1
          fi
          mkdir -p "$MEGAREPO_STORE"
          echo "Using job-local megarepo store: $MEGAREPO_STORE"
          if [ -n "${GITHUB_ENV:-}" ]; then
            printf 'MEGAREPO_STORE=%s\n' "$MEGAREPO_STORE" >> "$GITHUB_ENV"
          fi

          nix run "github:overengineeringstudio/effect-utils/$EU_REV#megarepo" -- apply --all
        shell: bash
      - name: Prepare CI helper scripts
        shell: bash
        run: |
          set -euo pipefail
          scripts_src='repos/livestore/genie/ci-scripts'
          scripts_dst='${{ runner.temp }}/composition-state/ci-runtime'
          if [ ! -d "$scripts_src" ]; then
            echo "::error::CI helper script directory is missing after megarepo sync: $scripts_src"
            exit 1
          fi
          rm -rf "$scripts_dst"
          mkdir -p "$scripts_dst"
          cp -R "$scripts_src/." "$scripts_dst/"
          rm -f "$scripts_dst"/*.genie.ts
          chmod +x "$scripts_dst"/*.sh
      - name: Use pinned devenv from lock
        run: |
          cd "${GITHUB_WORKSPACE:-$PWD}" && DEVENV_REV=$(jq -r .nodes.devenv.locked.rev 'devenv.lock')
          if [ -z "$DEVENV_REV" ] || [ "$DEVENV_REV" = "null" ]; then
            printf '::error::%s missing .nodes.devenv.locked.rev\n' 'devenv.lock'
            exit 1
          fi
          echo "DEVENV_REV=$DEVENV_REV" >> "$GITHUB_ENV"
          echo "Pinned devenv rev: $DEVENV_REV"
        shell: bash
      - name: Isolate pnpm state
        shell: bash
        run: |
          cd "${GITHUB_WORKSPACE:-$PWD}" && set -euo pipefail
          mkdir -p "${{ runner.temp }}/composition-state/pnpm-store-pure-v1" "${{ runner.temp }}/composition-state/pnpm-home" .devenv
          member_store=.devenv/pnpm-store-pure-v1
          if [ -L "$member_store" ]; then
            if [ "$(readlink "$member_store")" != "${{ runner.temp }}/composition-state/pnpm-store-pure-v1" ]; then
              echo "::error::owned member pnpm store symlink has the wrong target: $member_store" >&2
              exit 1
            fi
          elif [ -e "$member_store" ]; then
            echo "::error::refusing non-symlink owned member pnpm store path: $member_store" >&2
            exit 1
          else
            ln -s "${{ runner.temp }}/composition-state/pnpm-store-pure-v1" "$member_store"
          fi
          echo "PNPM_STORE_DIR=${{ runner.temp }}/composition-state/pnpm-store-pure-v1" >> "$GITHUB_ENV"
          echo "PNPM_CONFIG_STORE_DIR=${{ runner.temp }}/composition-state/pnpm-store-pure-v1" >> "$GITHUB_ENV"
          echo "PNPM_HOME=${{ runner.temp }}/composition-state/pnpm-home" >> "$GITHUB_ENV"
      - id: restore-pnpm-state
        name: Restore pnpm state
        uses: actions/cache/restore@v4
        with:
          path: |
            ${{ runner.temp }}/composition-state/pnpm-home
            ${{ runner.temp }}/composition-state/pnpm-store-pure-v1
          key: "livestore-contrib-pnpm-state-v1-v3-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('**/pnpm-lock.yaml') }}"
      - name: Resolve devenv
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          __genie_ci_retry_script='${{ runner.temp }}/composition-state/ci-runtime/run-with-nix-gc-race-retry.sh'
          bash "$__genie_ci_retry_script" 'resolve devenv (devenv.lock)' 'cd "${GITHUB_WORKSPACE:-$PWD}" && '"'"'${{ runner.temp }}/composition-state/ci-runtime/resolve-devenv.sh'"'"' '"'"'devenv.lock'"'"''
        shell: bash
      - name: Install workspace dependencies
        run: |
          __genie_ci_retry_script='${{ runner.temp }}/composition-state/ci-runtime/run-with-nix-gc-race-retry.sh'
          bash "$__genie_ci_retry_script" 'devenv tasks run pnpm:install' 'cd "${GITHUB_WORKSPACE:-$PWD}" && if [ -n "${NIX_CONFIG:-}" ]; then NIX_CONFIG_WITH_APPEND=$(printf '"'"'%s\n%s'"'"' "$NIX_CONFIG" '"'"'restrict-eval = false'"'"'); else NIX_CONFIG_WITH_APPEND='"'"'restrict-eval = false'"'"'; fi; NIX_CONFIG="$NIX_CONFIG_WITH_APPEND" DEVENV_TASK_PASSTHROUGH=1 DEVENV_TUI=false "${DEVENV_BIN:?DEVENV_BIN not set}" tasks run pnpm:install'
      - name: Plan and enforce adoption gate
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
          DOCS_CORRELATION_KEY: ${{ secrets.DOCS_CORRELATION_KEY }}
          ADMIN_TOKEN: ${{ secrets.ADMIN_TOKEN }}
          E2E_ACTOR_TOKEN: ${{ secrets.E2E_ACTOR_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          set -euo pipefail
          DEVENV_TASK_PASSTHROUGH=1 DEVENV_TUI=false "${DEVENV_BIN:?DEVENV_BIN not set}" shell --no-reload -- bash -euo pipefail -c '
            cd apps/discord-bot
            pnpm cf:plan --stage "$STAGE" 2>&1 | tee "$RUNNER_TEMP/discord-bot-plan.log"
            node --experimental-strip-types cf/scripts/check-deploy-plan.ts "$RUNNER_TEMP/discord-bot-plan.log"
          '
      - name: Deploy approved staging plan
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
          DOCS_CORRELATION_KEY: ${{ secrets.DOCS_CORRELATION_KEY }}
          ADMIN_TOKEN: ${{ secrets.ADMIN_TOKEN }}
          E2E_ACTOR_TOKEN: ${{ secrets.E2E_ACTOR_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          AGENT_ACTION_APPROVAL: deploy
        run: |
          set -euo pipefail
          DEVENV_TASK_PASSTHROUGH=1 DEVENV_TUI=false "${DEVENV_BIN:?DEVENV_BIN not set}" shell --no-reload -- bash -euo pipefail -c '
            cd apps/discord-bot
            pnpm cf:deploy --stage "$STAGE" --yes
          '
      - name: Verify gateway readiness and release
        run: |
          node --input-type=module <<'NODE'
          import { setTimeout } from 'node:timers/promises'
          const url = new URL('/readyz', process.env.CF_WORKER_URL)
          if (url.protocol !== 'https:' || !process.env.CF_WORKER_URL || !process.env.RELEASE_ID) {
            throw new Error('CF_WORKER_URL must be an HTTPS URL and RELEASE_ID must be set')
          }
          const checks = ['journalCurrent', 'supervisorReady', 'sessionPresent', 'gatewayHealthy', 'errorFree']
          const deadline = Date.now() + 120_000
          while (Date.now() < deadline) {
            try {
              const response = await fetch(url, { signal: AbortSignal.timeout(5000) })
              if (response.status === 200) {
                const report = await response.json()
                if (report.ready === true && report.releaseId === process.env.RELEASE_ID &&
                    checks.every((key) => report.checks?.[key] === true)) {
                  console.log('Discord bot ready at expected release', report.releaseId)
                  process.exit(0)
                }
              }
            } catch (error) {
              console.log('Readiness request failed:', error instanceof Error ? error.message : String(error))
            }
            await setTimeout(5000)
          }
          throw new Error('Discord bot readiness did not reach expected release within 120 seconds')
          NODE
      - name: Upload Nix diagnostics artifact
        if: failure() && env.NIX_STORE_DIAGNOSTICS_DIR != ''
        uses: actions/upload-artifact@v4
        with:
          name: 'nix-store-diagnostics-${{ github.job }}-${{ runner.os }}-run-${{ github.run_id }}-attempt-${{ github.run_attempt }}'
          path: ${{ env.NIX_STORE_DIAGNOSTICS_DIR }}
          if-no-files-found: ignore
          retention-days: 14
