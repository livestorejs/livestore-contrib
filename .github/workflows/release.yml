# Generated file - DO NOT EDIT
# Source: release.yml.genie.ts

name: Release

on:
  workflow_dispatch:
    inputs:
      mode:
        description: Release workflow mode
        required: true
        default: validate-release-surface
        type: choice
        options: [validate-release-surface, publish-snapshot]
  workflow_run:
    workflows: [ci]
    types: [completed]
    branches: [main]
  pull_request: {}
  push:
    branches: [main]

permissions:
  contents: read
  id-token: write

env:
  CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
  CI: 'true'
  FORCE_SETUP: '1'

jobs:
  release-surface:
    runs-on: [namespace-profile-linux-x86-64, 'namespace-features:github.run-id=${{ github.run_id }}']
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v6
      - name: Prepare CI helper scripts
        shell: bash
        run: |
          set -euo pipefail
          scripts_src='genie/ci-scripts'
          scripts_dst='${{ runner.temp }}/genie-ci-scripts'
          if [ ! -d "$scripts_src" ]; then
            echo "::error::CI helper script directory is missing: $scripts_src"
            exit 1
          fi
          rm -rf "$scripts_dst"
          mkdir -p "$scripts_dst"
          cp -R "$scripts_src/." "$scripts_dst/"
          chmod +x "$scripts_dst"/*.sh
      - name: Install Nix
        uses: DeterminateSystems/determinate-nix-action@v3
        with:
          extra-conf: |
            experimental-features = nix-command flakes
            accept-flake-config = true
            extra-substituters = https://devenv.cachix.org
            extra-trusted-public-keys = devenv.cachix.org-1:w1cLUi8dv3hnoSPGAuibQv+f9TZLr6cv/Hm9XgU50cw=
            access-tokens = github.com=${{ github.token }}
            extra-substituters = https://cache.nixos.org
            extra-trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY=
          summarize: true
      - name: Sync megarepo dependencies
        env:
          MEGAREPO_STORE: '${{ runner.temp }}/megarepo-store/${{ github.run_id }}/${{ github.run_attempt }}/${{ github.job }}'
        run: |
          EU_REV=$(jq -r '.members["effect-utils"].commit' megarepo.lock)
          if [ -z "$EU_REV" ] || [ "$EU_REV" = "null" ]; then
            echo '::error::megarepo.lock missing members["effect-utils"].commit'
            exit 1
          fi
          mkdir -p "$MEGAREPO_STORE"
          echo "Using job-local megarepo store: $MEGAREPO_STORE"
          if [ -n "${GITHUB_ENV:-}" ]; then
            printf 'MEGAREPO_STORE=%s\n' "$MEGAREPO_STORE" >> "$GITHUB_ENV"
          fi

          nix run "github:overengineeringstudio/effect-utils/$EU_REV#megarepo" -- apply --all --worktree-mode commit --lock-sync off
        shell: bash
      - name: Use pinned devenv from lock
        run: |
          DEVENV_REV=$(jq -r .nodes.devenv.locked.rev devenv.lock)
          if [ -z "$DEVENV_REV" ] || [ "$DEVENV_REV" = "null" ]; then
            echo '::error::devenv.lock missing .nodes.devenv.locked.rev'
            exit 1
          fi
          echo "DEVENV_REV=$DEVENV_REV" >> "$GITHUB_ENV"
          echo "Pinned devenv rev: $DEVENV_REV"
        shell: bash
      - name: Isolate pnpm state
        shell: bash
        run: |
          echo "PNPM_STORE_DIR=${{ github.workspace }}/.pnpm-store" >> "$GITHUB_ENV"
          echo "PNPM_CONFIG_STORE_DIR=${{ github.workspace }}/.pnpm-store" >> "$GITHUB_ENV"
          echo "PNPM_HOME=${{ github.workspace }}/.pnpm-home" >> "$GITHUB_ENV"
      - id: restore-pnpm-state
        name: Restore pnpm state
        uses: actions/cache/restore@v4
        with:
          path: |
            ${{ github.workspace }}/.pnpm-home
            ${{ github.workspace }}/.pnpm-store
          key: "livestore-contrib-pnpm-state-v1-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('**/pnpm-lock.yaml') }}"
      - name: Resolve devenv
        run: |
          DEVENV_REV=$(jq -r .nodes.devenv.locked.rev devenv.lock)
          if [ -z "$DEVENV_REV" ] || [ "$DEVENV_REV" = "null" ]; then
            echo '::error::devenv.lock missing .nodes.devenv.locked.rev'
            exit 1
          fi

          . '${{ runner.temp }}/genie-ci-scripts/resolve-devenv.sh'

          # Temporary: capture diagnostics dir for #272 root-cause analysis.
          DIAG_ROOT="${RUNNER_TEMP:-/tmp}/nix-store-diagnostics-${GITHUB_JOB:-job}-${RUNNER_OS:-unknown}-${GITHUB_RUN_ATTEMPT:-0}"
          mkdir -p "$DIAG_ROOT"
          echo "NIX_STORE_DIAGNOSTICS_DIR=$DIAG_ROOT" >> "$GITHUB_ENV"

          {
            echo "timestamp_utc=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo "runner_name=${RUNNER_NAME:-unknown}"
            echo "runner_os=${RUNNER_OS:-unknown}"
            echo "runner_arch=${RUNNER_ARCH:-unknown}"
            echo "github_job=${GITHUB_JOB:-unknown}"
            echo "github_run_id=${GITHUB_RUN_ID:-unknown}"
            echo "nix_user_conf_files=${NIX_USER_CONF_FILES:-}"
            nix --version || true
          } > "$DIAG_ROOT/environment.txt" 2>&1

          if ! DEVENV_OUT=$(resolve_devenv 2> >(tee "$DIAG_ROOT/resolve-devenv.log" >&2)); then
            echo "::error::resolve_devenv failed. Last 30 lines of log:"
            tail -30 "$DIAG_ROOT/resolve-devenv.log" || true
            exit 1
          fi
          DEVENV_BIN="$DEVENV_OUT/bin/devenv"

          # Fast validity check on the devenv store path (~1-2s vs ~25s for devenv info).
          if ! nix-store --check-validity "$DEVENV_OUT" 2>/dev/null; then
            echo "::warning::devenv store path invalid, repairing targeted path..."
            nix-store --repair-path "$DEVENV_OUT" > "$DIAG_ROOT/nix-store-verify-repair.log" 2>&1 || true
            rm -rf "${XDG_CACHE_HOME:-$HOME/.cache}"/nix/eval-cache-* ~/.cache/nix/eval-cache-*
            if ! DEVENV_OUT=$(resolve_devenv 2> >(tee "$DIAG_ROOT/resolve-devenv-post-repair.log" >&2)); then
              echo "::error::resolve_devenv failed after repair. Last 30 lines of log:"
              tail -30 "$DIAG_ROOT/resolve-devenv-post-repair.log" || true
              exit 1
            fi
            DEVENV_BIN="$DEVENV_OUT/bin/devenv"
          fi

          # resolve_devenv_once uses this out-link directly, so Nix registers the GC
          # root before a successful build returns. RUNNER_TEMP cleanup removes it after
          # the job; the run/attempt/job namespace prevents cross-job replacement.
          if [ ! -L "$DEVENV_GC_ROOT" ] || [ ! "$DEVENV_GC_ROOT" -ef "$DEVENV_OUT" ]; then
            echo "::error::devenv resolution did not publish the expected job-scoped GC root: $DEVENV_GC_ROOT"
            exit 1
          fi
          echo "DEVENV_GC_ROOT=$DEVENV_GC_ROOT" >> "$GITHUB_ENV"

          echo "DEVENV_BIN=$DEVENV_BIN" >> "$GITHUB_ENV"
          "$DEVENV_BIN" version | tee "$DIAG_ROOT/devenv-version.txt"
        shell: bash
      - name: Validate release surface
        run: |
          __genie_ci_retry_script='${{ runner.temp }}/genie-ci-scripts/run-with-nix-gc-race-retry.sh'
          bash "$__genie_ci_retry_script" 'devenv tasks run release:surface:check' 'if [ -n "${NIX_CONFIG:-}" ]; then NIX_CONFIG_WITH_APPEND=$(printf '"'"'%s\n%s'"'"' "$NIX_CONFIG" '"'"'restrict-eval = false'"'"'); else NIX_CONFIG_WITH_APPEND='"'"'restrict-eval = false'"'"'; fi; NIX_CONFIG="$NIX_CONFIG_WITH_APPEND" PNPM_HOME="${PNPM_HOME:-${{ github.workspace }}/.pnpm-home}" PNPM_STORE_DIR="${PNPM_STORE_DIR:-${{ github.workspace }}/.pnpm-store}" PNPM_CONFIG_STORE_DIR="${PNPM_CONFIG_STORE_DIR:-${{ github.workspace }}/.pnpm-store}" DEVENV_TASK_PASSTHROUGH=1 DEVENV_TUI=false "${DEVENV_BIN:?DEVENV_BIN not set}" tasks run release:surface:check'
      - name: Save pnpm state
        if: ${{ success() && steps.restore-pnpm-state.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@v4
        with:
          path: |
            ${{ github.workspace }}/.pnpm-home
            ${{ github.workspace }}/.pnpm-store
          key: "livestore-contrib-pnpm-state-v1-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('**/pnpm-lock.yaml') }}"
      - name: Upload Nix diagnostics artifact
        if: failure() && env.NIX_STORE_DIAGNOSTICS_DIR != ''
        uses: actions/upload-artifact@v4
        with:
          name: 'nix-store-diagnostics-${{ github.job }}-${{ runner.os }}-run-${{ github.run_id }}-attempt-${{ github.run_attempt }}'
          path: ${{ env.NIX_STORE_DIAGNOSTICS_DIR }}
          if-no-files-found: ignore
          retention-days: 14
  publish-snapshot-version:
    if: (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push') || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && inputs.mode == 'publish-snapshot')
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      id-token: write
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
      - name: Prepare CI helper scripts
        shell: bash
        run: |
          set -euo pipefail
          scripts_src='genie/ci-scripts'
          scripts_dst='${{ runner.temp }}/genie-ci-scripts'
          if [ ! -d "$scripts_src" ]; then
            echo "::error::CI helper script directory is missing: $scripts_src"
            exit 1
          fi
          rm -rf "$scripts_dst"
          mkdir -p "$scripts_dst"
          cp -R "$scripts_src/." "$scripts_dst/"
          chmod +x "$scripts_dst"/*.sh
      - name: Install Nix
        uses: DeterminateSystems/determinate-nix-action@v3
        with:
          extra-conf: |
            experimental-features = nix-command flakes
            accept-flake-config = true
            extra-substituters = https://devenv.cachix.org
            extra-trusted-public-keys = devenv.cachix.org-1:w1cLUi8dv3hnoSPGAuibQv+f9TZLr6cv/Hm9XgU50cw=
            access-tokens = github.com=${{ github.token }}
            extra-substituters = https://cache.nixos.org
            extra-trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY=
          summarize: true
      - name: Sync megarepo dependencies
        env:
          MEGAREPO_STORE: '${{ runner.temp }}/megarepo-store/${{ github.run_id }}/${{ github.run_attempt }}/${{ github.job }}'
        run: |
          EU_REV=$(jq -r '.members["effect-utils"].commit' megarepo.lock)
          if [ -z "$EU_REV" ] || [ "$EU_REV" = "null" ]; then
            echo '::error::megarepo.lock missing members["effect-utils"].commit'
            exit 1
          fi
          mkdir -p "$MEGAREPO_STORE"
          echo "Using job-local megarepo store: $MEGAREPO_STORE"
          if [ -n "${GITHUB_ENV:-}" ]; then
            printf 'MEGAREPO_STORE=%s\n' "$MEGAREPO_STORE" >> "$GITHUB_ENV"
          fi

          nix run "github:overengineeringstudio/effect-utils/$EU_REV#megarepo" -- apply --all --worktree-mode commit --lock-sync off
        shell: bash
      - name: Use pinned devenv from lock
        run: |
          DEVENV_REV=$(jq -r .nodes.devenv.locked.rev devenv.lock)
          if [ -z "$DEVENV_REV" ] || [ "$DEVENV_REV" = "null" ]; then
            echo '::error::devenv.lock missing .nodes.devenv.locked.rev'
            exit 1
          fi
          echo "DEVENV_REV=$DEVENV_REV" >> "$GITHUB_ENV"
          echo "Pinned devenv rev: $DEVENV_REV"
        shell: bash
      - name: Isolate pnpm state
        shell: bash
        run: |
          echo "PNPM_STORE_DIR=${{ github.workspace }}/.pnpm-store" >> "$GITHUB_ENV"
          echo "PNPM_CONFIG_STORE_DIR=${{ github.workspace }}/.pnpm-store" >> "$GITHUB_ENV"
          echo "PNPM_HOME=${{ github.workspace }}/.pnpm-home" >> "$GITHUB_ENV"
      - id: restore-pnpm-state
        name: Restore pnpm state
        uses: actions/cache/restore@v4
        with:
          path: |
            ${{ github.workspace }}/.pnpm-home
            ${{ github.workspace }}/.pnpm-store
          key: "livestore-contrib-pnpm-state-v1-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('**/pnpm-lock.yaml') }}"
      - name: Resolve devenv
        run: |
          DEVENV_REV=$(jq -r .nodes.devenv.locked.rev devenv.lock)
          if [ -z "$DEVENV_REV" ] || [ "$DEVENV_REV" = "null" ]; then
            echo '::error::devenv.lock missing .nodes.devenv.locked.rev'
            exit 1
          fi

          . '${{ runner.temp }}/genie-ci-scripts/resolve-devenv.sh'

          # Temporary: capture diagnostics dir for #272 root-cause analysis.
          DIAG_ROOT="${RUNNER_TEMP:-/tmp}/nix-store-diagnostics-${GITHUB_JOB:-job}-${RUNNER_OS:-unknown}-${GITHUB_RUN_ATTEMPT:-0}"
          mkdir -p "$DIAG_ROOT"
          echo "NIX_STORE_DIAGNOSTICS_DIR=$DIAG_ROOT" >> "$GITHUB_ENV"

          {
            echo "timestamp_utc=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo "runner_name=${RUNNER_NAME:-unknown}"
            echo "runner_os=${RUNNER_OS:-unknown}"
            echo "runner_arch=${RUNNER_ARCH:-unknown}"
            echo "github_job=${GITHUB_JOB:-unknown}"
            echo "github_run_id=${GITHUB_RUN_ID:-unknown}"
            echo "nix_user_conf_files=${NIX_USER_CONF_FILES:-}"
            nix --version || true
          } > "$DIAG_ROOT/environment.txt" 2>&1

          if ! DEVENV_OUT=$(resolve_devenv 2> >(tee "$DIAG_ROOT/resolve-devenv.log" >&2)); then
            echo "::error::resolve_devenv failed. Last 30 lines of log:"
            tail -30 "$DIAG_ROOT/resolve-devenv.log" || true
            exit 1
          fi
          DEVENV_BIN="$DEVENV_OUT/bin/devenv"

          # Fast validity check on the devenv store path (~1-2s vs ~25s for devenv info).
          if ! nix-store --check-validity "$DEVENV_OUT" 2>/dev/null; then
            echo "::warning::devenv store path invalid, repairing targeted path..."
            nix-store --repair-path "$DEVENV_OUT" > "$DIAG_ROOT/nix-store-verify-repair.log" 2>&1 || true
            rm -rf "${XDG_CACHE_HOME:-$HOME/.cache}"/nix/eval-cache-* ~/.cache/nix/eval-cache-*
            if ! DEVENV_OUT=$(resolve_devenv 2> >(tee "$DIAG_ROOT/resolve-devenv-post-repair.log" >&2)); then
              echo "::error::resolve_devenv failed after repair. Last 30 lines of log:"
              tail -30 "$DIAG_ROOT/resolve-devenv-post-repair.log" || true
              exit 1
            fi
            DEVENV_BIN="$DEVENV_OUT/bin/devenv"
          fi

          # resolve_devenv_once uses this out-link directly, so Nix registers the GC
          # root before a successful build returns. RUNNER_TEMP cleanup removes it after
          # the job; the run/attempt/job namespace prevents cross-job replacement.
          if [ ! -L "$DEVENV_GC_ROOT" ] || [ ! "$DEVENV_GC_ROOT" -ef "$DEVENV_OUT" ]; then
            echo "::error::devenv resolution did not publish the expected job-scoped GC root: $DEVENV_GC_ROOT"
            exit 1
          fi
          echo "DEVENV_GC_ROOT=$DEVENV_GC_ROOT" >> "$GITHUB_ENV"

          echo "DEVENV_BIN=$DEVENV_BIN" >> "$GITHUB_ENV"
          "$DEVENV_BIN" version | tee "$DIAG_ROOT/devenv-version.txt"
        shell: bash
      - name: Select snapshot versions
        run: |
          set -euo pipefail
          core_sha="$(jq -r '.members.livestore.commit' megarepo.lock)"
          if [ -z "$core_sha" ] || [ "$core_sha" = "null" ]; then
            echo "megarepo.lock is missing members.livestore.commit" >&2
            exit 1
          fi
          contrib_sha="$(git rev-parse HEAD)"
          echo "GIT_SHA=$contrib_sha" >> "$GITHUB_ENV"
          echo "LIVESTORE_CORE_GIT_SHA=$core_sha" >> "$GITHUB_ENV"
          echo "LIVESTORE_CORE_RELEASE_VERSION=0.0.0-snapshot-$core_sha" >> "$GITHUB_ENV"
          echo "LIVESTORE_RELEASE_VERSION=0.0.0-snapshot-$core_sha.$contrib_sha" >> "$GITHUB_ENV"
      - name: Publish contrib snapshot version
        run: |
          __genie_ci_retry_script='${{ runner.temp }}/genie-ci-scripts/run-with-nix-gc-race-retry.sh'
          bash "$__genie_ci_retry_script" 'devenv tasks run release:snapshot:git-sha' 'if [ -n "${NIX_CONFIG:-}" ]; then NIX_CONFIG_WITH_APPEND=$(printf '"'"'%s\n%s'"'"' "$NIX_CONFIG" '"'"'restrict-eval = false'"'"'); else NIX_CONFIG_WITH_APPEND='"'"'restrict-eval = false'"'"'; fi; NIX_CONFIG="$NIX_CONFIG_WITH_APPEND" PNPM_HOME="${PNPM_HOME:-${{ github.workspace }}/.pnpm-home}" PNPM_STORE_DIR="${PNPM_STORE_DIR:-${{ github.workspace }}/.pnpm-store}" PNPM_CONFIG_STORE_DIR="${PNPM_CONFIG_STORE_DIR:-${{ github.workspace }}/.pnpm-store}" DEVENV_TASK_PASSTHROUGH=1 DEVENV_TUI=false "${DEVENV_BIN:?DEVENV_BIN not set}" tasks run release:snapshot:git-sha'
      - name: Save pnpm state
        if: ${{ success() && steps.restore-pnpm-state.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@v4
        with:
          path: |
            ${{ github.workspace }}/.pnpm-home
            ${{ github.workspace }}/.pnpm-store
          key: "livestore-contrib-pnpm-state-v1-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('**/pnpm-lock.yaml') }}"
      - name: Upload Nix diagnostics artifact
        if: failure() && env.NIX_STORE_DIAGNOSTICS_DIR != ''
        uses: actions/upload-artifact@v4
        with:
          name: 'nix-store-diagnostics-${{ github.job }}-${{ runner.os }}-run-${{ github.run_id }}-attempt-${{ github.run_attempt }}'
          path: ${{ env.NIX_STORE_DIAGNOSTICS_DIR }}
          if-no-files-found: ignore
          retention-days: 14
