require_version: '>=2.1'
inputs:
  devenv:
    url: github:cachix/devenv/v2.1
  nixpkgs:
    follows: effect-utils/nixpkgs
  effect-utils:
    # Pinned to the revision megarepo.lock records; mr:lock-sync-check requires
    # every Nix lock revision of a megarepo member to match it.
    url: github:overengineeringstudio/effect-utils/165e63eefb6ccb3e70bddc6d0ba0272be25976fe
