import { describe, expect, it } from 'vitest'

// Remove once we have actual tests (needed to please `vitest`)
describe('placeholder', () => {
  it('should pass', () => {
    expect(true).toBe(true)
  })
})
